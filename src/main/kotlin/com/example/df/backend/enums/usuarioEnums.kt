package com.example.df.backend.enums

enum class StatusConta{ATIVA, SUSPENSA, INATIVA}
enum class TipoUser{user,admin,sub_adm} // sub_adm é para caso de alguem precisar de autoridade sómente para algo
// especifico como por exemplo add nova obra e somente isso..